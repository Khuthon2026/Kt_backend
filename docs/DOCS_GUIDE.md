# 성과 문서화 가이드 (PT용)

> 팀장이 "문서화해줘"라고 요청할 때만 실행합니다.
> 그 시점까지 구현된 내용을 코드베이스 기반으로 한 번에 정리하세요.
> PT에서 그대로 읽을 수 있도록 한국어로 작성합니다.

## 문서 구조

docs/
├── api-performance.md     # API 응답 속도, 처리량
├── db-performance.md      # 쿼리 성능, 인덱스 효과
└── feature-summary.md     # 구현 기능 목록 및 기술적 의사결정

---

## 포맷

### `docs/api-performance.md`

| 엔드포인트 | 메서드 | 예상 응답시간 | 인증 필요 | 비고 |
|------------|--------|--------------|-----------|------|
| `/api/users/{id}` | GET | ~50ms | ✅ | DB 단건 조회 |

---

### `docs/db-performance.md`

| 쿼리 대상 | 인덱스 적용 | 예상 실행시간 | N+1 여부 | 비고 |
|-----------|------------|--------------|----------|------|
| User 전체 조회 | ✅ email | ~10ms | ❌ fetch join | - |

---

### `docs/feature-summary.md`

## [기능명]
- 구현 방식: (핵심 로직 한 줄 요약)
- 기술적 선택: (왜 이 방식을 택했는지)
- 처리 가능 규모: (동시 요청 수, 데이터 건수 등 추정치)

---

## 작성 원칙
- 수치는 추정치도 괜찮으나 근거 병기 (예: "DB 조회 1회 기준 ~30ms 예상")
- Before/After가 있으면 반드시 비교 형태로 작성